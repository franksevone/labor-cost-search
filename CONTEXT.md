# CONTEXT — ทุกสิ่งที่ต้องรู้เกี่ยวกับโฟลเดอร์นี้

> เอกสารนี้คือ "จุดเริ่มต้น" สำหรับใครก็ตาม (มนุษย์ หรือ AI) ที่เข้ามาในโฟลเดอร์นี้
> อ่านจบแล้วควรเข้าใจ: โปรเจกต์คืออะไร · ข้อมูลมาจากไหน · โครงสร้างไฟล์อย่างไร ·
> เคยแก้อะไรไปแล้วบ้าง · จะ rebuild / deploy อย่างไร

---

## 1. โปรเจกต์นี้คืออะไร

**ระบบค้นหาบัญชีค่าแรงงานก่อสร้าง** — เว็บ static ไฟล์เดียว (`index.html`) สำหรับค้นหาราคาค่าแรง
งานก่อสร้างจากเอกสารราชการต้นฉบับ:

**บัญชีค่าแรงสำหรับถอดแบบคำนวณราคากลางงานก่อสร้าง**
(หนังสือกรมบัญชีกลาง ด่วนที่สุด ที่ กค 0433.2/ว 480 ลงวันที่ 26 มิถุนายน 2569)

- ทำงานแบบ **offline 100%** — เปิด `index.html` ด้วยเบราว์เซอร์ได้เลย ไม่ต้องติดตั้งอะไร
- ข้อมูลทั้งหมดฝังอยู่ใน `data.js` (ไม่มี API / backend / database)
- มีฟีเจอร์: ค้นหาข้อความ + คำพ้อง/คำย่อ (~150 กลุ่ม), กรองตามหมวด/ประเภท,
  ตารางเหมือนเอกสารต้นฉบับ (รวมรายการหลายระดับราคา), เลือกรายการ + พิมพ์สรุป (localStorage)

## 2. ตัวเลขปัจจุบันของข้อมูล (อัปเดตล่าสุด)

| รายการ | จำนวน |
| --- | --- |
| หมวดหมู่ (1–5) | 5 |
| ประเภท (เช่น 1.1, 2.4, 4.58) | 104 |
| รายการ (item) | **1,285** (มีราคา 1,268) |
| ราคา (unit + ราคา + หมายเหตุ) | **1,471** |

โครงสร้างข้อมูล: `หมวดหมู่ → ประเภท → รายการ → ราคา[]`
รายการมี `prices[]` หลายระดับ (เช่น เสาเข็ม 100 ต้นขึ้นไป / 50–100 / 25–50)
พร้อมหัวข้อย่อย: `sections` (หมวดย่อย เช่น "1.2.1 ค่าตอกเสาเข็มคอนกรีตในเขต กทม.")
และ `groups` (กลุ่มภาษาอังกฤษ เช่น "Ceiling mounted Type")
ประเภทบางประเภทมี `notes[]` = หมายเหตุ/เชิงอรรถท้ายตารางของประเภทนั้น

## 3. ไฟล์ในโฟลเดอร์ — บทบาทแต่ละไฟล์

| ไฟล์ | บทบาท | หมายเหตุ |
| --- | --- | --- |
| `index.html` | หน้าเว็บระบบค้นหา (ของจริงที่ใช้งาน) | static ล้วน อ้างอิงแค่ `data.js` |
| `data.js` | ข้อมูลที่เว็บใช้ (`window.LABOR_DATA = …`) | **generated** จาก build_data.py |
| `data.json` | ข้อมูลโครงสร้างเดียวกันใน JSON | **generated** จาก build_data.py |
| `build_data.py` | สคริปต์หลัก: PDF → data.json + data.js | ใช้ PyMuPDF (pymupdf) |
| `extract.py` | ดึงข้อความ PDF → extracted.txt (ขั้นกลาง) | ส่วนใหญ่ใช้เพื่อ debug/วิเคราะห์ |
| `extracted.txt` | ข้อความจาก PDF แยกหน้า (มี `===== PAGE n =====`) | **generated** จาก extract.py |
| `validate.py` | ตรวจคุณภาพ data.json (หน่วยหาย? ชื่อว่าง? ฯลฯ) | รันหลัง build ทุกครั้ง |
| `บัญชีค่าแรง….pdf` | เอกสารต้นฉบับจากกรมบัญชีกลาง | ข้อมูลอ้างอิงอันดับหนึ่ง |
| `README.md` | คู่มือผู้ใช้ + ข้อมูลทั่วไป | |
| `SUMMARY.md` | สรุปโครงการ + ไอเดียต่อยอด (ติดตามงาน) | |
| `CONTEXT.md` | เอกสารนี้ — ทุกสิ่งที่ต้องรู้ | |
| `.venv/` | Python virtualenv (มี pymupdf) | ไม่ commit ขึ้น git |

**ลำดับ pipeline:** `PDF → (extract.py) → extracted.txt → (build_data.py) → data.json/data.js → (validate.py) → ตรวจผ่าน`

## 4. วิธี rebuild / ตรวจสอบข้อมูล (เมื่อแก้อะไรใน build_data.py)

```bash
# 1. สร้างข้อมูลใหม่ (เขียนทับ data.json + data.js)
.venv/Scripts/python.exe build_data.py

# 2. ตรวจคุณภาพ
.venv/Scripts/python.exe validate.py

# 3. เช็คว่า data.js เป็น JS ที่เว็บอ่านได้
node -e "global.window={}; require('./data.js'); console.log(window.LABOR_DATA.categories.length)"
```

ข้อควรรู้:
- ต้องมี Python + PyMuPDF ใน `.venv` (`py -m venv .venv` + `pip install PyMuPDF`)
- เวลารันใน Windows console อาจต้อง `PYTHONIOENCODING=utf-8` ข้างหน้า เพื่อกันภาษาไทยเพี้ยน
- **ตัวเลขที่ต้องเฝ้า:** หลัง rebuild ต้องได้ `items=1285 prices=1471 missing_unit=0`
  (ตัวเลขนี้ตรงกับข้อมูลปัจจุบัน — ถ้าเปลี่ยนแสดงว่ามีอะไรเปลี่ยนจริง)

## 5. ประวัติการแก้ไขที่สำคัญ (อ่านก่อนแตะ build_data.py!)

### 5.1 ฟอนต์ PDF ผิดเพี้ยน (อ่านไม่ออก) — แก้ตั้งแต่แรก
- ฟอนต์ตัวหนาใน PDF แมป "า" เป็น "ำ" ผิด และ "ำ" แท้หายไป
- แก้ใน `fix_line()`: ใช้กฎ `เว้นวรรค+สระ = ำ แท้`, `ำ ลอย ๆ ในฟอนต์หนา = า`
- ตรวจผล: `extracted.txt` ครบ 45 หน้า อ่านภาษาไทยถูกต้อง

### 5.2 เงื่อนไขราคาหลุดไปเป็น "ชื่อรายการ" (บั๊กใหญ่ 2 รอบ)
**รอบแรก:** `TIER_KEYWORDS` (จำนวน/ตั้งแต่/น้อยกว่า…) เดาไม่หมด — เงื่อนไขที่ขึ้นต้น
"ภายใน/ภายนอก/พื้นที่" (เช่น 2.8.1 ค่าทาสีน้ำพลาสติก 4 ระดับ) กลายเป็นรายการแยก
→ **รอบสอง (แก้จริง):** เลิกเดาคำขึ้นต้น เปลี่ยนเป็น **แยกคอลัมน์ด้วยตำแหน่ง x0 ใน PDF**:
- `x0 ≥ 370` = คอลัมน์**หมายเหตุ** → เก็บเป็นหมายเหตุของราคาแถวนั้นเสมอ (ตรวจแล้วแยก 100% ไม่มีเส้นกำกวม)
- ข้อความคอลัมน์รายการที่ขึ้นต้น `(`, `หรือ`, `ชนิด`, `ขนาด` = **รายละเอียดต่อท้ายชื่อ** → ต่อกับชื่อเดิม
- ผลลัพธ์: รายการลดจาก 1,321 → 1,285 (รายการหลอกหาย 36), ราคาคงที่ 1,471
- ตัวอย่างที่แก้: 2.7.1 งานบันได (5 ระดับราคาเป็นรายการเดียว), 1.6.2 คอนกรีตโครงสร้าง (3 ระดับ),
  2.5.x วงกบ/บานประตู, 2.4.16 พื้นไม้ ฯลฯ

### 5.3 เชิงอรรถท้ายตารางปนเข้า "หมายเหตุราคา" (โซน 2.9 + อื่น ๆ)
- เชิงอรรถท้ายตาราง (เช่น "รื้อกอง หมายถึง…", "หมายเหตุ มากกว่า 5,000 ตร.ม.…") เดิมติดเข้า
  หมายเหตุของราคาแถวสุดท้าย → แยกออกเป็น `type.notes` แทน
- **เจอเพิ่ม:** pymupdf คายเชิงอรรถออกมา "หลัง" ตำแหน่งจริงในหน้า (block order ≠ ตำแหน่ง y)
  → เชิงอรรถทาสีของ 2.8 ไปติดใน 2.9, เชิงอรรถ `*ปูนทราย…ขณะติดตั้ง` ของ 2.4 ไปติดใน 2.5
- แก้ใน `page_lines()`: เก็บค่า y ทุกบรรทัด แล้ว **ย้ายเชิงอรรถ (เส้นขึ้นต้น `หมายเหตุ <ข้อความ>`)**
  ไปไว้ก่อนหัวข้อประเภทถัดไป (รหัส 2 ส่วน) ที่อยู่ใต้มัน → ผูกกับประเภทที่ถูกต้อง
- ผลลัพธ์: 2.4 มี 3 notes · 2.5 ไม่มี · 2.8 มี 1 (เชิงอรรถทาสี) · 2.9 มี 2 (รื้อกอง/รื้อขนไป)

### 5.4 ข้อผิดพลาดอื่น ๆ ในเอกสารต้นฉบับ (แก้ใน build_data.py)
- รหัส "2.68" ใน PDF = จริง ๆ คือ "2.6.8" (`CODE_TYPOS`)
- "3.3" ใน PDF เป็นประเภทที่ไม่มีชื่อ → ใช้ `MISPLACED_TITLES` ตั้งชื่อให้

### 5.5 หมายเหตุ/เชิงอรรถที่ "รู้แล้วว่าเป็นเช่นนั้น" (ไม่ใช่บั๊ก — เอกสารเป็นแบบนั้น)
- มีหมายเหตุท้ายตารางบางจุดที่ยังติดอยู่กับหมายเหตุราคา (เช่น footnote ภาษาอังกฤษ `** เฉพาะค่าตัดจากโรงงาน…`)
- รายการ `1.2.1.15 ตัดหัวเสาเข็มคอนกรีต` ไม่มีราคา (container) — ตั้งใจ
- "สิ่งที่ส่งมาด้วยตามหนังสือ…" เป็นข้อความท้ายทุกหน้า → ตัดทิ้งใน parser

## 6. การ deploy ขึ้นออนไลน์ (GitHub Pages) — ✅ ทำแล้ว

**URL: https://franksevone.github.io/labor-cost-search/**
**Repo: https://github.com/franksevone/labor-cost-search** (public, branch `main`)

- เปิด Pages แบบ **Deploy from a branch** (main / root) — ตรวจแล้ว `index.html` + `data.js` ตอบ 200
- การอัปเดต: แก้ไฟล์ → `git add -A && git commit && git push` → Pages อัปเดตอัตโนมัติ (~1-2 นาที)
  (**ต้อง push `data.js`/`data.json` ใหม่ทุกครั้งที่ rebuild ข้อมูล** — หน้าเว็บอ่าน `data.js`)
- ตรวจสถานะ: `gh api repos/franksevone/labor-cost-search/pages` (status ควรเป็น `built`)
- โฟลเดอร์นี้เป็น git repo แล้ว (init ครั้งแรก 16 ส.ค. 2569)

## 7. สถานะปัจจุบัน

- ✅ ข้อมูลตรงกับ PDF ต้นฉบับ (ตรวจเทียบแล้วหลายรอบ)
- ✅ validate.py ผ่าน · data.js โหลดใน node ได้
- ✅ พร้อมใช้งาน offline (เปิด index.html) และ online (GitHub Pages) — **https://franksevone.github.io/labor-cost-search/**
- ✅ เป็น git repo แล้ว (remote: origin → github.com/franksevone/labor-cost-search)
- 📌 ตัวเลขเฝ้าระวัง: items 1,285 · prices 1,471 · missing_unit 0

## 8. ไอเดียต่อยอด (ดูรายละเอียดใน SUMMARY.md)

- คำนวณยอดรวมค่าแรงในหน้าพิมพ์สรุป / ส่งออก Excel · เลือกทั้งหมดหน้าปัจจุบัน ·
- ค้นหาแบบฟัซซี่ · กรองช่วงราคา · บันทึกชุดรายการที่เลือกหลายชุด · เปรียบเทียบราคาเคียงกัน
